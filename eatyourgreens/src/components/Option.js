import React from 'react'

//left box OR align top on mobile 

export const Option = ({label}) => {

    return (
        <div>
            <button>
                {label}
            </button>
        </div>
    )
}