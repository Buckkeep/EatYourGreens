import React from 'react'

//left box OR align top on mobile 

export const Choices = ({label}) => {

    return (
        <div>
            <button>
                {label}
            </button>
        </div>
    )
}