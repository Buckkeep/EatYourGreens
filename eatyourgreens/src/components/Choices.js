import React from 'react'

function Choices() {
    let label;
    return (
        <div>
            <button>This is the Choices component { label }</button>
        </div>
    )
}

export default Choices